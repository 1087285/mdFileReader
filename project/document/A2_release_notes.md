# A2 リリースノート

## 1. 文書情報

| 項目 | 内容 |
|------|------|
| 文書名 | リリースノート |
| プロジェクト | mdFileReader |
| 最新版 | v1.1.0 |
| 作成日 | 2026-03-02 |
| 作成者 | GitHub Copilot（08_release_agent） |

---

## 2. v1.1.0（2026-03-02）

### 概要

Shift-JIS エンコードの Markdown ファイルを読み込んで文字化けなく表示・保存できる機能を追加。

### 変更点

| 変更 | 説明 |
|------|------|
| Shift-JIS 読み込み対応 | chardet で `shift_jis` を検出した場合に `cp932`（Windows 互換）に正規化して正しくデコード |
| エンコード保持保存 | 読み込み時に検出したエンコード（cp932 / utf-8 等）のまま保存。新規ファイルは UTF-8 |
| ENCODE_SAVE_ERROR | Shift-JIS ファイルに絵文字等 cp932 非対応文字を入力し保存しようとした場合、エラーを表示しファイルを保護 |

### テスト結果

| 区分 | 件数 | 結果 |
|------|------|------|
| 単体テスト（pytest） | 24 | ✅ 全 PASS |
| 結合テスト（pytest） | 21 | ✅ 全 PASS |
| GUI 受入テスト（実機） | 20 | ⏳ 実機確認待ち |

### 変更ファイル

- `project/src/file_service.py`
- `project/src/backend_bridge.py`
- `project/src/resources/app.js`

---

## 3. v1.0.0（2026-03-02）

### 概要

Markdown ファイルの表示・編集・保存ができるスタンドアロンデスクトップアプリケーション `mdFileReader` の初回リリース。

### 新機能

| 機能 | 説明 |
|------|------|
| ファイルツリー表示 | 選択フォルダ配下の `.md` ファイルを左ペインにツリー表示 |
| Markdown エディタ | CodeMirror 5.65.16 によるシンタックスハイライト付き |
| リアルタイムプレビュー | marked.js による Markdown → HTML プレビュー |
| 文字コード自動判定 | chardet による UTF-8 / Shift-JIS 等の自動検出 |
| ファイル CRUD | 新規作成・削除（確認ダイアログ）・名前変更（同名確認ダイアログ） |
| 保存 | `Ctrl+S` / 保存ボタン、UTF-8 BOM なしで書き込み |
| セキュリティ | パストラバーサル防止（BaseDir 外アクセスをブロック） |
| スタンドアロン動作 | PyInstaller 生成 `.exe` 単一ファイル、インストール不要 |

### 動作環境

- Windows 10 / Windows 11
- インストール不要（任意フォルダに `.exe` を配置して実行）

### 既知の制約

| # | 制約 | 暫定対応 |
|---|------|----------|
| 1 | `createFolder` スロット未実装 | ツリー右クリック「新規ファイル」でフォルダ内に `.md` ファイルを作成するとフォルダが自動生成される（`.gitkeep` ワークアラウンド） |
| 2 | Linux / macOS 非対応 | 次バージョン以降で検討 |

### テスト結果

| 区分 | 件数 | 結果 |
|------|------|------|
| 単体テスト（pytest） | 22 | ✅ 全 PASS |
| 結合テスト（pytest） | 19 | ✅ 全 PASS |
| GUI 受入テスト（実機） | 17 | ⏳ 実機確認待ち |

### リリース URL

| 項目 | 内容 |
|------|------|
| GitHub Releases | （実機ビルド・リリース後に記入） |
| タグ | v1.0.0 |

---

## 3. 更新チェックリスト（リリース担当者）

- [ ] Windows 実機で `pyinstaller mdFileReader.spec` 実行 → `dist/mdFileReader.exe` 生成確認
- [ ] `ACC-OK-01～13` 正常系受入テスト全件手動確認
- [ ] `ACC-NG-01～07` 異常系受入テスト全件手動確認
- [ ] `git tag v1.1.0 && git push origin v1.1.0` 実行
- [ ] GitHub Releases 本体作成（本文に本ファイル内容を貼付）
- [ ] `mdFileReader.exe` を GitHub Releases にアタッチ
- [ ] リリース URL を A1/A2 の「リリース URL」欄に記入

